apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-repository-generator.fullname" . }}
  labels:
    {{- include "argocd-repository-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-repository-generator.selectorLabels" . | nindent 4 }}
